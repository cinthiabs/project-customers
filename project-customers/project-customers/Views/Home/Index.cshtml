@model IEnumerable<ClientesModel>
@{
    ViewData["Title"] = "Home Page";
}
<link href="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/css/bootstrap.min.css" rel="stylesheet">
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.6.0/jquery.min.js"></script>
<script src="https://cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/4.6.0/js/bootstrap.bundle.min.js"></script>


<div class="container">

    <div class="row">
        <div class="col-md-8">
            <p>Consulte os seus Clientes cadastrados na sua Loja ou realize o cadastro de novos Clientes.</p>
        </div>
        <div class="col-md-4 text-right">
            <button class="btn btn-primary" onclick="window.location.href = '/Home/Cliente';">Adicionar Cliente</button>
        </div>
    </div>

    <button class="btn btn-secondary mt-3" id="filtro-modal" data-toggle="modal" data-target="#filtroModal">Filtrar</button>

    <div class="modal fade" id="filtroModal" tabindex="-1" role="dialog" aria-labelledby="filtroModalLabel" aria-hidden="true">
            <div class="modal-content-style">
                <div class="modal-header">
                    <h5 class="modal-title" id="filtroModalLabel">Filtros</h5>
                    <button type="button" class="close" data-dismiss="modal" aria-label="Fechar">
                        <span aria-hidden="true">&times;</span>
                    </button>
                </div>
                    <div class="modal-body">
                        <form id="filterForm" method="post" asp-action="ImportaExcel" asp-controller="Home" enctype="multipart/form-data" class="text-center d-flex flex-column mt-3" style="display: none;">
                            <div class="input-container row">
                                <!-- Nome/Razão Social -->
                                <div class="form-group col-md-4">
                                    <label for="nome_razao_social">Nome/Razão Social:</label>
                                    <input type="text" class="form-control" id="nome_razao_social" name="nome_razao_social" placeholder="Nome ou Razão Social do Cliente" />
                                </div>

                                <!-- E-mail -->
                                <div class="form-group col-md-4">
                                    <label for="email">E-mail:</label>
                                    <input type="text" class="form-control" id="email" name="email" placeholder="E-mail do Cliente" />
                                </div>

                                <!-- Telefone -->
                                <div class="form-group col-md-4">
                                    <label for="telefone">Telefone:</label>
                                    <input type="text" class="form-control" id="telefone" name="telefone" placeholder="Telefone do Cliente" />
                                </div>

                                <!-- Data de Cadastro -->
                                <div class="form-group col-md-4">
                                    <label for="data_cadastro">Data de Cadastro:</label>
                                    <input type="date" class="form-control" id="data_cadastro" name="data_cadastro" placeholder="Selecione uma data ou período" />
                                </div>

                                <!-- Cliente Bloqueado -->
                                <div class="form-group col-md-4">
                                    <label for="cliente_bloqueado">Cliente Bloqueado:</label>
                                    <select class="form-control" id="cliente_bloqueado" name="cliente_bloqueado">
                                        <option value="">Selecione</option>
                                        <option value="0">SIM</option>
                                        <option value="1">NÃO</option>
                                    </select>
                                </div>

                                <div class="row mt-3">
                                    <div class="button-container">
                                        <button class="btn btn-primary">Aplicar Filtros</button>
                                        <button class="btn btn-secondary">Limpar Filtros</button>
                                    </div>
                                </div>
                        </form>
                    </div>
            </div>
        </div>
    </div>


    <table class="table table-striped table-hover mt-5" id="toggleTable">
        <thead>
            <tr>
                <th>Razão Social</th>
                <th>E-mail</th>
                <th>Telefone</th>
                <th>Data Cadastro</th>
                <th>Situação Cliente</th>
                <th>Ações</th>
            </tr>
        </thead>
        <tbody>
            @if (Model != null && Model.Count() > 0)
            {
                @foreach (var cliente in Model)
                {
                    <tr>
                        <td>@cliente.RazaoSocial</td>
                        <td>@cliente.Email</td>
                        <td>@cliente.Telefone</td>
                        <td>@cliente.DataCadastro</td>
                        <td>@cliente.StatusCliente</td>
                      
                        <td><button class="btn btn-danger" id="actions" >Editar</button></td>
                    </tr>
                }
            }
            else
            {
                <tr>
                    <td colspan="6" class="text-center">Sem clientes cadastrados!</td>
                </tr>
            }

        </tbody>
    </table>
</div>

<script>
    /*
        function ExcluirProduto(id) {
        if (confirm('Tem certeza que deseja excluir este produto?')) {
            $.ajax({
                type: "POST",
                url: '@Url.Action("ExcluirProduto", "Home")',
                data: { id: id },
                success: function () {
                    window.location.reload();
                },
                error: function () {
                    alert('Ocorreu um erro ao tentar excluir o produto.');
                }
            });
        }
    }
   function ExcluirTodos() {
        if (confirm('Tem certeza que deseja excluir todos os produtos?')) {
            $.ajax({
                type: "POST",
                url: '@Url.Action("ExcluirTodos", "Home")',
                success: function () {
                    reloadPage();
                },
                error: function () {
                    reloadPage();
                }

            });
        }
    }

    function reloadPage() {
        location.reload(true);
    }*/

</script>